export default function Loading() {
  // You can add any UI inside Loading, including a Skeleton.
  return (
    <div className="fixed top-0 left-0 h-screen w-full bg-black/5  z-[1000] flex justify-center items-center">
      Loading...
    </div>
  );
}
